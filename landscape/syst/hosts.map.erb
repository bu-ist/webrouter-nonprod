legacy <%= ENV['BACKEND_WEB_LEGACY'] || "ist-web-legacy-#{ENV['LANDSCAPE']}.bu.edu" %> ;

old_content <%= ENV['BACKEND_WEB_CONTENT'] || "ist-web-content-#{ENV['LANDSCAPE']}.bu.edu" %> ;

wpassets <%= ENV['BACKEND_WP_CONTENT'] || "ist-wp-assets-#{ENV['LANDSCAPE']}.bu.edu" %> ;

wordpress <%= ENV['BACKEND_WP_APP'] || "ist-wp-app-#{ENV['LANDSCAPE']}.bu.edu" %> ;

dbin ist-web-dbin-devl.bu.edu ;

phpbin ist-web-phpbin-devl.bu.edu ;

degree-advice <%= ENV['BACKEND_DEGREE_ADVICE'] || "degree-advice-#{ENV['LANDSCAPE']}.bu.edu" %> ;

django ist-web-django-devl.bu.edu ;

uiscgi_app <%= ENV['BACKEND_UISCGI_APP'] || "ist-uiscgi-app-#{ENV['LANDSCAPE']}.bu.edu" %> ;

uiscgi_content <%= ENV['BACKEND_UISCGI_CONTENT'] || "ist-uiscgi-content-#{ENV['LANDSCAPE']}.bu.edu" %> ;

content ist-w3-content-syst101.bu.edu ;
content-nocache ist-w3-content-syst101.bu.edu ;
content-cache ist-w3-content-syst101.bu.edu ;

# ###
# The following backend types provide the homepage in S3.
# There are multiple entries because the the homepage needs to map to 
# a subdirectory on the S3 bucket.
#
# This aws_homepage bucket is used for everything that maps normally:
aws_home homepage-devl.s3-website-us-east-1.amazonaws.com ;

# These entries are necessary to map to specific URLs in the /home space:
#
aws_home_index homepage-devl.s3-website-us-east-1.amazonaws.com/home/index.html ;

